# 更新记录

## 4.1.0 — 2026-09-06

### 移动端

新增窄屏也可见的横屏提示，包括横放示意、全屏入口和关闭按钮；横屏自动隐藏，再次转为竖屏可重新提示。提示位于原生全屏元素内部，不阻断触控、不暂停对局、不强制锁定屏幕方向。ASCII 下以文本符号替代提示图标。

### 全屏

原生全屏仍覆盖浏览器允许的显示区域。球场改为最大化容纳的 16:9 居中矩形，空余区域留黑边，横纵等比、不拉伸、不裁切。画布、ASCII 和触控共享球场位置；背景缓存与主画布的像素宽高比也保持 16:9。HUD 按球场高度而非屏幕高度缩放；考虑浏览器报告的安全区内边距。

### 光影

重写图形渲染器，加入光学玻璃材质、蚀刻刻度、金属导光球拍、动态侧向光照、连续渐细尾迹、球体高光、分层碰撞反馈与得分光效。仍提供精致光影/均衡画质。背景缓存复用，粒子与波纹数量设上限，装饰随机数独立于游戏。减少动态效果偏好关闭相关粒子与运动装饰。

### 保留

DOM ASCII 的低开销绘制路径、文字节点复用、静止时按需重绘；快速真人球拍、高频功能球、AI 无增益；发球防重开、手动 P2P 与云房间逻辑。切换显示与全屏不改变物理或联机协议。

### 验证

75 项浏览器回归检查通过；另提供 3 组各约 10 秒的禁用 GPU ASCII 采样。未进行实体手机系统全屏、双击文件导航、线上 Pages 或公网双设备端到端实测。
