# 更新记录

## 4.0.0 — 图形 / ASCII 分离与原生满屏

- 图形模式增加实际尺寸高分辨率画布、双色发光球拍、球场背景缓存、拖尾、碰撞粒子、得分和功能球光效。
- 精致光影与均衡画质独立控制，不再继承 ASCII 30 帧节能设置。
- ASCII 保留零图形上下文冷启动、变化行更新、静止时按需重绘；切回时释放图形缓存。
- 新用户默认图形模式，升级保留已有偏好；另附始终先进入字符模式的 `ascii_start.html`，以及 `?render=ascii` 参数。
- 原生全屏球场覆盖视口，移除外围面板、边距和固定宽高比留边；提供自动隐藏的浮动控制栏。
- 全屏菜单和暂停面板按需显示，拒绝全屏时给出实际提示，不用 CSS 冒充原生全屏。
- 保留 960×540 逻辑物理；非 16:9 全屏按宽高分别映射，触控同步映射。
- 保留空格发球防重开、真人 1100 移速、高频功能球、人机无增益以及手动 P2P / 云房间联机。
- 55 项浏览器回归通过；新增三组禁用 GPU 的 ASCII 冷启动软件渲染采样。测试未覆盖实体设备或公网双端。
