# v3.0.0

## 界面

重写主界面、球场容器、比分栏、工具栏、模式选择、设置区、暂停 / 对局状态区和联机表单样式。桌面采用双栏控制台；竖屏自然滚动，横屏对局自动收起侧栏。全屏范围同时包含场地与操作控件；按实际可用高度适配球场。

移除强制横屏遮挡和方向锁定。保留主题颜色、键盘与触屏操作、两种联机方式及高级网络设置。

## 渲染

默认 ASCII；持久文本行与复用字节缓冲区、变化行更新、有限字符密度、延迟 Canvas 初始化、不生成字符模式拖尾。移除整套 CRT 噪声、闪烁、扫描、模糊与发光开销。

新增自适应、60 帧、30 帧节能档及本机性能摘要。绘制节流不改变物理步长、键盘移速、球速和增益规则；静止状态按需重绘。

## 交互与修复

保留空格不重开对局与防重复发球处理。倒计时可以暂停并恢复原阶段；本地游戏进入后台自动暂停。修复触控 `pointercancel` 也触发发球的问题，只有正常 `pointerup` 才请求发球。

## 验证边界

完成软件渲染性能测试和本地逻辑 / 界面回归。没有完成真实两台设备、公网 PeerJS、TURN 或线上 Pages 的端到端连接实测。详见测试报告。
