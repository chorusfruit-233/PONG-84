# PONG-84 v5.0.0 · 测试与交付记录

日期：2026-09-06。基线：本次对话的 `index_optical_16x9.html`（16:9 光学球场版）。交付入口：`index.html` 与 `ascii_start.html`。

## 结论与边界

本次共完成 **140 项功能／协议检查，另完成 7 项发布检查**，记录中均通过且未捕获未处理的页面 JavaScript 异常。它们不是 147 次真实联网对局。

四人大厅、物理、同步、准备屏障与恢复协议已完成自动化检查。**没有完成四台实体设备、真实四端 ICE 数据通道、公共 PeerJS、TURN 或线上 GitHub Pages 的端到端联机验证。** 不据此承诺任意网络连通或任意低配置 CPU 恒定 60 FPS。

实际 `file://` 导航被运行环境的浏览器策略拒绝：`ERR_BLOCKED_BY_ADMINISTRATOR`。没有更改或绕过浏览器管理策略。界面测试通过 `page.set_content` 注入完整 HTML 运行；这验证页面代码和浏览器 API 行为，不能替代文件 URL 导航测试。

生产手动建连路径未被改写，真实尝试报告：“浏览器没有提供可用的连接地址，请检查 WebRTC 权限、防火墙或 TURN 设置。” 浏览器未产出可用 ICE candidate。本次没有把空 candidate 的连接码作为可用邀请码交付。

## 分组结果

| 测试组 | 通过 | 方法与含义 | 记录 |
|---|---:|---|---|
| 原功能回归 | 75 / 75 | Chromium 真实 DOM、Fullscreen API；单打／AI／渲染测试场景 | `validation/regression.json` |
| 四端协议与物理 | 35 / 35 | 四个独立 Chromium 上下文；**模拟消息桥，非实际 WebRTC 传输** | `validation/bridge_report.json` |
| 原生 SDP 与校验 | 15 / 15 | 真实 WebRTC Offer/Answer API＋边界检查；**未接通 ICE** | `validation/native_report.json` |
| 双打界面与生命周期 | 15 / 15 | DOM、原生全屏、PointerEvent 和固定比赛场景 | `validation/extras_report.json` |
| 发布入口检查 | 7 / 7 | 独立 ASCII 文件、软件渲染特征、可复现构建和文件一致性 | `validation/release_checks.json` |

另外保留 `file_navigation.json` 和 `native_environment.json` 的真实失败记录；这些是未完成项／环境限制，不计为“连通通过”。

### 原有模式回归：75 项

覆盖图形画质切换、ASCII 冷启动和文本行复用、图形缓存释放、空格／回车发球不重开、人机增益限制、暂停状态冻结、原生全屏与拒绝提示、6 种尺寸下两种画面等比留边、移动端竖屏提示及关闭／方向变化、触控坐标与取消事件、减少动态效果偏好等。

原有 `OnlinePeer` 与 `InputManager` 实现的指纹与基线一致。四人网络管理在新类中独立实现，不表示把旧双人模式的网络协议整体重写为四人版。

### 四个独立浏览器上下文：35 项

执行生产 `DoublesRoom`、`DoublesGame`、`DoublesUI` 和物理代码。测试桥只替换数据通道传输：各页面将消息送入显式队列，由 Python 转交其他上下文。没有把浏览器上下文数称为“实体设备数”。

覆盖：四人加入和唯一身份、席位变更／换位同意、房主换到 B2、设置变更取消准备、非房主不能改规则、四人准备与开局确认、四个分区移动、错误席位发球拒绝、正确发球与单球同步、暂停冻结、独立画质、4 根球拍的扫掠碰撞、接缝单次加速、长拍四人可获得但每次只影响一根、团队护盾、失分队和队内交替发球、NaN／旧序号／错误回合输入拒绝、快照校验、单链路背压隔离、单人掉线、原席位令牌恢复、全状态继续、人为丢弃 20% 实时消息、房主后台冻结和主动退出中断。

20% 丢包仅为消息桥定期丢弃实时消息，**不是**真实网络中 20% 丢包的吞吐、延迟、SCTP 重传或 NAT 测试。不由此推导抗网络损伤性能指标。

### 原生 SDP：15 项

首先运行未经修改的生产邀请生成逻辑并记录失败。由于该环境没有 ICE candidate，为继续检查浏览器真实的 `createOffer`、`setLocalDescription`、`setRemoteDescription` 与 `createAnswer` API，**仅在测试页面内覆盖了 `collectIce` 的“必须有 candidate”门槛**。真实应用文件没有此覆盖。

三组独立主机 `RTCPeerConnection` 的信令状态到达 `stable`，控制通道有序、实时通道无序且 `maxRetransmits=0`。错误来宾回应、重复回应、被替换的邀请编号、旧协议和单打连接码均被拒绝；重建 G3 邀请未替换 G1、G2 的连接对象。

同时检查云端满员／版本不匹配／开局锁房／错误恢复令牌的应用层拒绝，以及云端实时通道的原生配置。这里的云端拒绝测试使用构造的连接事件，不等于访问实际公共 PeerServer 成功。

**所有记录中的 candidate 数量为 0。`stable` 是信令状态，不是 `connected`；本组不声称传输了任何真实 P2P 游戏数据。**

### 双打界面与生命周期：15 项

固定四人场景检查图形渲染、准备大厅、暂停恢复按钮的位置、空格不重开、重连 30 秒到期、移动端提示、不横向溢出、图形／ASCII 触控映射与取消事件，以及控制通道积压触发暂停时不会递归进入通知回调。

原生全屏测量：

| 可用区域 | 球场区域 | 留边 |
|---|---|---|
| 1920 × 1080 | 1920 × 1080 | 无 |
| 1440 × 900 | 1440 × 810 | 上下各 45 |
| 2560 × 1080 | 1920 × 1080 | 左右各 320 |
| 1024 × 768 | 1024 × 576 | 上下各 96 |

测量的是真实浏览器的全屏元素及 DOM 矩形，但不是实体手机的系统全屏行为。展示截图来自固定测试场景，不是联网实战截图。

## ASCII 与软件渲染

新增四端和双打检查使用 Chromium `--disable-gpu`。CDP `SystemInfo.getInfo` 记录显示 GPU 合成与光栅化均为 `disabled_software`，WebGL／WebGPU 为禁用；详见 `validation/software_gpu.json`。

7 项发布检查直接加载最终 `ascii_start.html` 内容，即使存储中原有图形偏好也进入 ASCII。四球拍房主大厅创建的 Canvas 上下文数为 **0**，真实 `.ascii-row` 文本节点存在，未出现页面运行异常。它不是通过修改程序默认画面后的替代入口测试。

这证明所测路径没有调用图形上下文，并在软件渲染浏览器中运行；不是所有无 GPU 实体设备的帧率保证。本版未发布四人模式的端到端帧率、CPU 节流吞吐或公网延迟实测值，也没有沿用旧双人性能数字冒充四人测试。

## 构建与交付

使用 Python 标准库执行 `tools/build.py`，由 `src/base.html` 和双打模块重建两个 HTML。`node --check` 检查合并后的 JavaScript 语法。重建前后 `index.html` 字节完全一致；单独交付的 `index_doubles.html` 与包内 `index.html` 字节一致。

`validation/release_checks.json` 保存最终 SHA-256。`SHA256SUMS.txt` 可用于检查发布包中的游戏入口、源文件与文档。构建失败会报告未匹配的补丁位置，不会静默生成不完整游戏。

## 后续实机验收清单

发布到面向陌生人的线上环境之前，仍需实际完成：四台设备同一局域网手动联机；三名来宾使用不同网络的云端联机；有效 TURN 的强制中继及混合直连／中继；公网长回合、真实丢包和带宽积压；Android／iOS 文件打开与原生全屏差异；房主切后台、设备断网／恢复、刷新和会话存储限制；四端混合图形／ASCII 及低端房主负载。

当前实现不包含自动房主迁移、AI 补位、观战和同机两人加入同一双打房间，也不包含生产 TURN 服务或短期凭据签发后端。
