# PONG-84 6.3.0 测试报告

本报告对应团队协议 v5 的协同强攻版。测试运行于真实桌面 Chromium，页面使用完整 HTML 注入加载；逻辑比赛使用生产固定步长与碰撞代码。未将模拟网络替身、短时画面提交采样或脚本对手当作真实设备公网联机／真人对战结果。

## 检查汇总

本次 **297 项检查**，另做发布可重建性检查。样本数与断言数分开统计。

| 检查组 | 断言数 | 主要范围 |
| --- | ---: | --- |
| 原有模式与 UI | 75 | 图形、ASCII、原生全屏、16:9、移动端提示、按键与触控 |
| 团队协议 | 70 | 同机双人、观众权限、投票、迁移、接管、状态确认及输入隔离 |
| 团队界面 | 37 | 大厅、准备、同机键盘／触控、组队操作、软件路径负载 |
| 增益与数学轨迹 | 49 | 同等增益、240 组生产碰撞对照、身份交接、有限状态 |
| 新 AI 控制与协作 | 48 | 延迟观察、转向惯性、速度、主接与补位、让球、战术反馈及迁移 |
| 固定窗口对照 | 12 | 180 s 窗口、旧版对照、拍心策略消融、脚本玩家 |
| 完整比赛与延长观察 | 6 | 对截断样本重放、180 s 比分一致、最终比分与完整结束 |

原始 JSON 位于 `validation/`。独立分组中有聚合断言；240 组轨迹、128 个初始比赛样本不再分别累计为额外检查。

## 防守不再是实时落点追踪

新控制器读取至少 140 ms 之前的可见状态，按 135–162 ms 的间隔决策；没有近球／变轨的高速紧急分支。无增益时 AI 峰值控制速度为 726，真人保持 1100。加速 4200、刹车 5200，单位为逻辑单位／秒²。测试验证突然反向目标不会让速度立刻翻转，单个移动物理步不能超出速度预算，且未来随机数不参与规划。

物理轨迹工具仍在 240 个指定场景中与生产碰撞对照；工具的数学精度不等于控制器具有实时观察能力。实际 AI 使用延迟状态和连贯估计误差。

## 完整 AI 自对战

每种阵型 16 场：8 个固定种子 × 道具／随机变轨关闭或启用。4 席均由 AI 控制，先得 7 分结束，不修改球速规则、碰撞、护盾或比分，不设置强制得分。初始比较窗口为 180 个模拟秒；未结束样本用同一条件重放，扩展观察上限为 360 秒，并核对 180 秒时比分一致。

| 阵型 | 最终完成 | 完整比赛时长中位数 | 完整比赛时长范围 | 原 180 s 窗口内完成 |
| --- | ---: | ---: | ---: | ---: |
| 上下协防 | 16 / 16 | 149.26 s | 48.36–203.17 s | 13 / 16 |
| 前锋＋后卫 | 16 / 16 | 59.48 s | 31.96–81.75 s | 16 / 16 |

**32 场新版样本最终均结束；并非所有比赛都在 180 秒内结束。** 原窗口内 29 场结束，3 场仍在比赛，比分分别为 6:5、4:6、5:6；延长后在约 194.46–203.17 秒结束。原始截断观察和初始“全部在 180 秒内完成”的未达成目标保存在 `development/`，没有用延长结果覆盖旧窗口。

作为对照，未修改的 6.2.0 在相同的 32 个种子／阵型／效果组合下，每场观察 180 秒，均仍为 0:0。旧版没有另做 360 秒窗口，因此不能宣称任何时长都绝对无法得分。这组结果仅说明本次指定条件下原有僵局被打破。

## 进攻消融与脚本对手

消融策略保留新版反应、速度和运动约束，但将进攻目标改为拍心接球。实测归一化绝对击球偏移（0 为拍心，1 为拍端）：

| 阵型 | 协同强攻 | 拍心消融 |
| --- | ---: | ---: |
| 上下协防 | 0.542 | 0.351 |
| 前后阵型 | 0.515 | 0.368 |

该结果证明出球偏移更积极，**不证明对任意对手胜率更高，也不说明每个回合一定更短**。原始比赛得分、失分和时长同时保留，不能只取较大偏移当作取胜证明。

另有 32 场脚本玩家队对照：脚本使用 140 ms 延迟观察、85 ms 决策间隔、1100 真人移速，通过生产移动函数和合法发球操作比赛，交替尝试正负 0.65 击球偏移。该脚本队在本组样本中均获胜。它仍使用数学落点计算，未模拟人类识别与操作误差，**不是普通玩家模型，更不是实测真人胜率**；这里只验证合法玩家控制能力足以击败新版 AI，而不是 AI 通过强制规则永远不败。

初始对照共 128 场：32 场旧版、32 场新版自对战、32 场拍心消融、32 场脚本玩家对照；额外 3 次为相同样本的延长重放，不计为 3 个独立随机种子。

## 配合与反馈

前后阵型验证前排主攻、后排补漏、球越过前排后转由后排负责；上下阵型验证单一主接者、合法半区和接缝分工。出球后采用互补站位。

让球分支的单元场景只替换了两条候选路线的比较分数，以隔离分支条件；后卫就绪检查、前排移动、速度边界和实际碰撞均使用生产代码。完整自对战另有自然触发的让球记录。未通过关闭碰撞或让来球直接穿过前排来完成让球。

得失分反馈可同时到达前一拍铺垫者和后一拍进攻者。对方拍边回球与拍心回球不因偏移不同而获得虚假的正向压迫奖励。迁移检查点保留有界速度、等待时间、角色和战术反馈，重建观察缓冲而不获得瞬时精确信息。

## 连得分快照修复

测试暴露旧校验只允许连得分计数不大于 3，但实际计分在持有护盾后仍可继续积累。例如 7:0 时可出现合法的连得分计数 4。已在团队快照校验中允许 0–99，与本游戏比分范围一致；越界值、非有限速度、非法角色目标、跨队战术归因仍被拒绝。

## 未完成与环境边界

- `file://` 导航被运行环境的管理员策略拦截，不能当作本地文件 URL 验收；页面检查使用新建浏览器页注入完整 HTML。
- 原生 WebRTC 诊断可以完成 SDP 协商，但两端可用 ICE 候选均为 0；正式手动邀请按预期拒绝这种无地址状态。未删除网络保护检查，也未把 SDP 成功当成连接成功，见 `validation/native_environment.json`。
- 团队协议测试经独立浏览器上下文和显式模拟消息转发驱动，不是真实跨设备 WebRTC。
- 未完成实体多人设备、公网 PeerJS、TURN、线上 GitHub Pages、实体 iOS／Android 原生全屏验收。
- ASCII 短样本测试记录在 `PERFORMANCE.md`，帧提交间隔不等同于实体屏幕帧率。
- 不承诺真人胜率，也不承诺所有随机局面均在固定时长内结束。以上完整比赛结论仅针对已记录样本。

## 复现

```sh
python tools/build.py
node --check validation/compiled.js
python tools/test_team_protocol.py
python tools/test_team_ui.py
python tools/test_team_ai.py
python tools/test_tactical_ai.py
python tools/regression.py
python tools/benchmark_tactical_ai.py
python tools/extend_tactical_matches.py
python tools/check_release.py
```

测试依赖和浏览器路径配置见 README。运行游戏本身不需要这些工具。`tools/test_attack.py` 保留为新控制器测试的兼容入口；旧测试及规则在 `history/v6.2/`，不与本版最终通过数混用。
