# 6.1.0 ASCII 与 AI 性能记录

对象：`ascii_start.html` 的团队模式，由本机作为权威主机，1 名真人 + 3 名 AI。工具：Chromium、`--disable-gpu`、DevTools CPU 节流；采样来源 `validation/team_ui.json`。测试自动为本机真人发球以继续采样，AI 发球仍使用生产逻辑。

GPU 状态查询确认：合成、光栅化和 2D Canvas 为软件路径，OpenGL / WebGL / WebGPU 关闭。游戏在两组采样中的 Canvas 上下文请求均为 0；并非请求了软件 Canvas 再把它称作纯文字。

| CPU 节流 | 采样时长 | 画面提交数 | 平均提交间隔 | P95 提交间隔 | Canvas 请求 |
| --- | --- | --- | --- | --- | --- |
| 1 倍 | 3.504 s | 210 | 16.66 ms | 18.40 ms | 0 |
| 6 倍 | 3.505 s | 204 | 17.13 ms | 26.20 ms | 0 |

这里测量的是脚本提交画面的节奏，不是实体显示器端到端帧率。样本短，不能推导任意旧设备均能恒定 60 FPS。实际设备负担过高时仍可使用 30 帧节能档，比赛物理步长不随绘制帧率下降。

## 预判开销控制

AI 普通规划间隔约 45 ms，接近来球或变轨时约 25 ms，并非每个 240 Hz 物理步重新跑完整预测。常见无旋转轨迹用解析折返公式；需要跟踪旋转或已知效果到期时，最多向前 2.5 秒、600 步。无未来随机采样，无 AI 额外 Canvas / WebGL / WebGPU 需求。

四 AI 的 90 秒加速模拟用于数值稳定性和移速边界检查，不能作为实际帧率测试。图形模式仍保留既有材质、光效和 Canvas 渲染，并未强制降成字符模式；本次不宣称新做了实体 GPU 图形性能验收。
