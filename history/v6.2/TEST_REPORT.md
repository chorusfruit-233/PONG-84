# PONG-84 6.2.0 测试报告

对象：本包 `index.html` / `ascii_start.html`，团队协议 v4。下列结果来自最终版本复测；当前原始记录在 `validation/`，旧记录单独保留于 `history/`。

## 功能检查

| 检查集 | 通过 / 总数 | 方法 |
| --- | --- | --- |
| 主动进攻、战术反馈与模拟对局 | 37 / 37 | 真实 Chromium 中运行生产物理，固定样例及带种子的本地模拟 |
| AI 同等增益、来球预判及恢复 | 66 / 66 | 同一物理状态比较身份；240 组来球轨迹对照 |
| 团队同步、观战、代打及房主迁移 | 70 / 70 | 独立浏览器上下文，显式内存消息转发，不是真实 WebRTC 网络 |
| 团队界面、同机输入、触控与 ASCII | 37 / 37 | 实际按键和 DOM；模拟手机触控；软件渲染采样 |
| 单打、原有输入、图形与 16:9 全屏 | 75 / 75 | 原有行为回归及基础源码哈希核对 |
| 合计 | **285 / 285** | 未捕获到页面 JavaScript 异常 |

样例组不额外算作断言数。旧版要求“球拍中心必须等于预测落点”的测试已改为检查预测落点本身及合法活动范围，因为本次目标正是让实际击球位置可以偏离中心；预测精度与速度边界检查仍保留。

## 攻击行为与规则一致性

四种阵型 / 方位固定场景中，规划选出的可达击球位置比中性直回具有更高的内部进攻评价，且不修改真实球或抽取随机数。100 组组合（4 席位 × 5 种球效果 × 5 种击球偏移）验证进攻模拟输出与真实 `resolvePaddle` 的球速、速度分量、半径、旋转及位置一致，误差阈值为 1e-7。

另验证：来不及移动时先救球、长拍即将到期、越过前锋后的后卫接球、半区边界、临近触球保持已选角度、从公开位置估计移动方向，以及 AI / 真人相同随机发球角度与速度。没有为取得进攻数据修改对方移速或真实碰撞规则。

## 固定追球型对手对照

对手规则是每 80 ms 观察一次当前球高并追踪，移速上限 1100；它是明确定义的模拟策略，不是真人，也不是旧版高速预判 AI。分别由两名 6.2 进攻 AI 或两名 6.1 防守 AI 对抗同一策略，开启正常随机功能球与变轨。

每个“阵型 × 测试策略”运行 8 局固定时长片段：4 个预先固定种子 × 左右镜像，每段 40 秒模拟时间。相同种子不意味着不同球路下所有随机事件都在同一时刻发生；因此是受控模拟对照而非完全逐事件配对实验。

| 阵型 | 6.1 防守策略累计得分 / 失分 | 6.2 进攻策略累计得分 / 失分 |
| --- | --- | --- |
| 上下协防 | 19 / 0 | 33 / 0 |
| 前锋 + 后卫 | 16 / 0 | 45 / 0 |

这说明新策略在此组样例中更能制造得分，不能换算为对真人胜率，也不能保证对任意策略都占优。

## 与高速预判旧 AI 直接交手

另进行了 32 段 25 秒本地模拟：两种阵型 × 有 / 无功能球及变轨 × 新 AI 左 / 右方 × 4 个固定种子。双方都保留 1100 移速和各自真实预测策略。这批短片段中双方均未取得得分（0:0）；不能据此声称进攻版必胜高速预判防守者。

不过击球方式发生了明确变化：新策略平均绝对击球偏移为 **0.493**，防守策略为 **0.160**（中心为 0、边缘为 1）。这只是击球行为指标，不是胜率。所有状态有限且合法；主动移动单步不超过 1100 / 240。道具伸长引起的边界位置修正与主动移动分开检查。

## 战术反馈与迁移

固定场景验证得分增加对应战术反馈、被轻松回球降低偏好、同局下一回合保留、新局清空、非法反馈值被快照校验拒绝。

模拟多设备迁移场景中，旧房主写入非零反向进攻反馈后提交检查点；旧房主失联，新房主获得观众见证票并恢复相同反馈计数与上次落点。旧协议 v3 的状态不能覆盖 v4。该检查经过真实房间消息处理代码，但传输为明确的内存替身，并非真实网络迁移实测。

## 保留的预判与单打

240 组来球轨迹仍与实际 `sweepD4Ball` 接触位置对照，全部在 0.05 逻辑单位容限内。上下与前后阵型各进行 90 秒四 AI 加速模拟；这不是实时显示帧率采样。

`src/base.html` 的 SHA-256 与 6.1 / 6.0 基础文件一致：

```text
0bf06906d11e5d0ff2812ceceaeaa5ca31e40fd1c3f7e693be2367063c69d3aa
```

因此单人人机无增益、单打难度、输入与图形 / ASCII 基础实现未因本次进攻改动而重写。

## 平台与真实网络边界

ASCII 主机（1 真人 + 3 AI）的两组采样使用禁用 GPU 的 Chromium，Canvas 上下文请求均为 0，详情见 [PERFORMANCE.md](PERFORMANCE.md)。弱 CPU 仍可能降帧，本报告不承诺所有设备恒定 60 FPS。

当前容器仍禁止 `file://` 导航（`ERR_BLOCKED_BY_ADMINISTRATOR`），且 WebRTC 未提供可用 ICE 候选地址。生产邀请生成正确拒绝无地址连接；原生 SDP 状态稳定不能证明连接成功。记录在 `validation/native_environment.json`。

没有完成实体手机、真实多设备局域网、公网 PeerJS / TURN、线上 GitHub Pages 或真实网络房主迁移的端到端验收，也没有真人胜率实验。未通过修改浏览器策略或移除生产检查绕过限制。

## 复跑

按 README 安装开发依赖后执行 `python tools/build.py`、`node --check validation/compiled.js`，再分别执行 `tools/test_attack.py`、`tools/test_team_ai.py`、`tools/test_team_protocol.py`、`tools/test_team_ui.py`、`tools/regression.py`、`tools/test_native_environment.py`。性能采样建议单独执行，避免其他套件抢占 CPU。
