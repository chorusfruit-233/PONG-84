# 6.2.0 ASCII 与主动进攻 AI 性能记录

本机作为权威主机，1 名真人 + 3 名主动进攻 AI，前后阵型。使用 Chromium 的实际生产帧循环与 ASCII DOM 渲染，`--disable-gpu`，并通过 DevTools 进行 CPU 节流。最后一次性能套件独立运行，没有同时运行其他项目测试套件。原始数据在 `validation/team_ui.json`。

| CPU 节流 | 时长 | 画面提交数 | 平均提交间隔 | P95 提交间隔 | Canvas 请求 |
| --- | --- | --- | --- | --- | --- |
| 1 倍 | 3.512 s | 211 | 16.66 ms | 18.10 ms | 0 |
| 6 倍 | 3.526 s | 194 | 18.16 ms | 27.70 ms | 0 |

GPU 状态查询显示合成、光栅化和 Canvas 为软件路径，OpenGL、WebGL 和 WebGPU 禁用。ASCII 本身没有请求 Canvas 上下文；图形模式仍使用原有 Canvas 和光影实现。

这些是短样本的脚本画面提交间隔，不是实体显示器端到端帧率。6 倍节流时已有掉帧，不应据此承诺极弱 CPU 始终 60 FPS；保留自适应与 30 帧节能档。模拟物理与绘制帧率仍然解耦。

## 计算边界

进攻只在权威主机计算，普通决策约 45 ms，紧急 / 变轨约 25 ms。每次至多搜索 17 个去重后的击球位置（13 个基本偏移、当前可达位置、安全区间两端及上次偏移）；每个候选检查对方两名球拍。直线无旋转可用解析折返；其余轨迹最多模拟 2.5 秒、600 个固定步。不会无界搜索或创建额外绘图上下文。

发球位置按 5 个候选站位、3 个代表角度评估，真实发球继续使用共同的随机角度规则。运动观测仅保存四名球拍最近的公开位置；反馈表只保存两队各六类有界数值。每回合丢弃候选缓存，新局清空反馈，避免长期积累。

进攻策略对比中的加速模拟耗时不能作为实时帧率证据。低端实机和实体手机仍需独立测试。
