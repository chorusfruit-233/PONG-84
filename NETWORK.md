# 联机、观战与房主迁移

适用：PONG-84 6.0.0，团队协议 v2。本文件描述当前代码行为；真实多设备网络验收未完成。

## 两台电脑、每台两人

两边都选择“四人双打”和“同机两人”。房主选择阵型、AI 补位与迁移选项，创建房间；另一台电脑通过同一云房间码或一组手动邀请码加入。每台电脑分别点击准备。游戏开始后，W/S 与空格属于本机玩家 1，方向键与回车属于本机玩家 2。两名本机玩家是同队，不是旧本地 PvP 的左右对抗。

要在房主突然失联后尝试自动继续，需要开局前再加入一台观众作为见证节点，且保留其页面与备用连接。只有两台设备时请使用有序“移交并退出”；突然失联后不能凑出两个投票。

## 观众加入

加入设备选择“只观战”，再输入房间码或手动邀请。观众最多四台，比赛途中也可加入。观众以 20 Hz 接收画面快照，参赛设备默认 60 Hz。观众不发送球拍控制、发球或暂停命令；房主仍验证消息来源，隐藏按钮不是唯一保护。

开局前已经在房间里的在线观众参与该局投票，不能成为主机。比赛中途加入的观众不修改正在进行的投票配置，从下一局起参与。观众退出可能影响当前局多数，所以不要把用于容错的观众节点随意关闭。

## 原生手动 P2P

设备入口一对一交换邀请和回应；一台设备上的两人共享这条入口。房主的每张来宾卡独立生成，不可把同一连接码发给多台设备。

局域网模式不加载 PeerJS，也不使用外部 STUN/TURN；跨网络模式使用设置中的 ICE 服务。配置 TURN 不等于自动可用，需验证地址、凭据、协议及服务额度。网页本身不提供 TURN 服务。

开启迁移后，主机利用已建立的可靠通道转发备用链路的 offer / answer。来宾之间建立新的原生 WebRTC 通道，每对拥有独立的连接票据；连接的实际 ICE 通路仍然需要可达。没有反向信令路径时无法凭空新建后备链路，所以在开局前准备，而非主机掉线后才开始。

八台设备全互联最多包含 28 对链路。它比原来的纯星形拓扑消耗更多连接资源；状态仍只由房主发送，备用链路并不各自跑一套比赛物理。低端设备或限制严格的网络可以在大厅关闭自动迁移。

## 云信令与新房间码

云联机仍由 PeerJS 负责初始信令。新团队房间使用独立的版本化 ID 前缀，避免和旧单双人协议误连。房主迁移成功后，新主机向信令服务申请新的四位码；活动成员不用重新手工入房，但旧码不保证继续作为可加入入口。

公共信令不可达时，已建立的数据通道可能继续工作；新的连接入口和重连可能失败。可查看“组队连接诊断”，区分信令失败、ICE 失败和备用链路不完整。

## 控制同步与迁移协议

比赛由当前房主统一计算。每个客户端只提交自己拥有的玩家 ID 的方向/触控目标，主机检验设备身份、序号、任期、局号和回合号。可靠通道承载准备、规则、发球、投票和检查点；实时通道无序、不重传，承载允许丢弃过时数据的输入/状态。每条连接分别检查发送积压，不让一个慢观众阻塞所有人。

迁移使用以下约束：

- 心跳约 450 ms；维持权威需得到当前投票组多数确认。失去租期后原主机停算。
- 按任期隔离旧权威消息。候选设备必须包含真人玩家且可用，观众仅投票。
- 完整检查点通常每 500 ms 发布，得到多数 ACK 才标为已提交。检查点包含比分、球拍、球、效果、发球轮次、回合、AI 内部计时等；不是客户端绘制插值后的画面。
- 新主机恢复最近提交点，处理掉线席位，向成员发起同步确认后倒计时继续。保留合法的效果与计时，但转为 AI 的席位立即应用无增益约束。

上述计时是实现参数，不是端到端恢复时限承诺。浏览器调度、网络排队和连接建立均会延长等待。协议针对非恶意合作客户端，未做形式化共识证明或 Byzantine 故障容错；没有防篡改权威服务器。

## 多数条件

本局投票组在开局时确定。需要 `floor(N/2)+1` 个投票设备仍能相互通信，且至少有一台合格玩家设备可以接管。

| 投票组规模 | 多数 | 可能容忍的失联数（仅按多数计算） |
| --- | --- | --- |
| 2 | 2 | 0 |
| 3 | 2 | 1 |
| 4 | 3 | 1 |
| 5 | 3 | 2 |
| 6 | 4 | 2 |
| 7 | 4 | 3 |
| 8 | 5 | 3 |

即使满足人数，也必须有备用链路、合格候选者和有效检查点；该表不是连通保证。两个分区不能各自擅自缩小投票组来宣布获胜。失去多数时保留冻结画面和原因，不继续写出互相冲突的比分。

## 有序移交

比赛内点击“移交房主”保留本人的席位，但把计算责任交给另一台在线参赛设备；点击“移交并退出”在交接完成后离开。过程需要旧房主发送授权、新房主确认。两台设备均在线时也支持。

若没有合格接班设备，无法保留对局并转移到观众。浏览器关闭、刷新、断电、进程被杀可能来不及发送最后一条消息，不能视为有序退出。短时切后台也可能触发暂停或 AI 接管；保持参赛/见证页面前台可降低不必要的中断。

## 断线与 AI

开启 AI 后：暂停活动或断线触发接管，原玩家身份暂时保留；恢复身份并完成同步后标记等待接回，下一次发球再变回真人。触控目标与持续按键在失效时清零，防止接管前后沿用陈旧输入。

关闭 AI 后：缺失真人导致冻结，等待回连或退出。刷新恢复依赖原标签页会话恢复记录及服务器入口仍可用；没有永久账户系统。新房主处理重连时使用迁移期间同步的恢复信息，不在诊断面板公开令牌。

## 故障排查

| 现象 | 检查方向 |
| --- | --- |
| 云组件下载或创房失败 | CDN / PeerJS 信令可达性；可改手动连接 |
| 邀请生成失败，提示没有可用地址 | 浏览器 WebRTC 策略、网卡/防火墙、局域网隔离、ICE 配置；不要删除地址检查来伪造成功 |
| 主链路正常但“备用连接”未完成 | 来宾两两之间的 ICE 路由；必要时配置 TURN 或在大厅关闭迁移 |
| 四个人已齐却不能开始 | 检查是否两台设备都准备、设置变更后需重新准备、备用链路/多数是否就绪 |
| 房主失联后一直冻结 | 是否保留原投票组多数、是否至少一名真人候选、是否有提交点；两台设备不够自动选主 |
| 迁移后旧四位码失效 | 使用新主机页面显示的当前码 |
| 玩家回连后球拍仍由 AI 控制 | 等待下一次发球；检查身份是否成功恢复、页面是否在前台 |
| 观众按键没反应 | 只读权限为预期；只能在大厅按席位可用情况转为参赛 |

## 官方技术资料

- [WebRTC 连接与信令](https://webrtc.org/getting-started/peer-connections)
- [WebRTC TURN 中继](https://webrtc.org/getting-started/turn-server)
- [MDN RTCDataChannel](https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel)
- [PeerJS 使用文档](https://peerjs.com/docs/)

上述资料解释平台机制，不构成本项目已通过公网测试的证据。
