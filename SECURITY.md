# 安全说明

## 使用边界

本项目适合熟人和合作客户端游戏，不是有反作弊保障的排位平台。主机统一裁决减少客户端分歧，但运行在玩家浏览器中的代码可以被修改；迁移票据和连接来源检查不等于对恶意房主的防护。

## 不要公开的内容

完整邀请 / 回应码可能包含网络地址和临时连接信息；恢复令牌用于找回席位。不要把这些内容、TURN 凭据或任何服务端 API 密钥上传到公开仓库或 Issue。提交诊断时删去真实 IP、域名中的私有信息和账号凭据。

公开 HTML 内不能安全保存长期服务密钥。需要公共 TURN 服务时，由可信服务端保管签发密钥并提供短期凭据；本包没有签发后端，不代为提供 TURN 服务。

四位码用于定位房间，不是访问密码。本版不提供公开互联网对抗性访客的完整身份认证、封禁或速率限制服务。观众的游戏控制权限由主机检查，但不要把此权限模型等同于安全审计通过。

## 数据与外部依赖

游戏本身不要求摄像头、麦克风或注册账号。显示偏好与昵称可保存在本地浏览器存储，恢复信息使用会话存储（是否可用受浏览器策略影响）。网络设置中的长期凭据不应写进代码；迁移需要向可继任玩家节点传递必要恢复身份信息。

本地玩法和手动局域网模式不依赖 PeerJS CDN。选择云模式会按需加载第三方脚本并连接信令服务；跨网络 ICE 使用设置中的服务。第三方依赖和服务器有独立的安全与可用性边界。

## 问题报告

本仓库未配置专门的安全响应邮箱。报告问题前去除连接码、令牌和账号信息；涉及可利用漏洞时，优先使用仓库所有者提供的私密联系方式，而非直接公布可利用细节。不存在已完成独立安全审计的声明。
