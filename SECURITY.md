# 公开仓库与联机安全

不要提交长期 TURN 用户密码、信令服务管理密钥、服务商 API 密钥、完整邀请／回应码或席位恢复令牌。邀请中的 SDP 可能包含网络地址信息，只应发给预期参加者。

四位房间码是房间定位符，不是高强度访问密码。此版没有私密房间密码、账号认证、竞技级反作弊或服务器端裁判。不要把它用于有奖、公平性要求严格的比赛。房主可以修改浏览器代码，主机权威不代表主机可信。

网络设置中的凭据仅在页面内存中使用，不写入游戏偏好存储。来宾恢复令牌可保存在当前标签页的 `sessionStorage`；关闭标签页或拒绝存储会影响恢复。诊断输出不含恢复令牌和 TURN 密码，但昵称和房间标识仍可能显示，公开分享前应检查。

使用第三方云信令或 TURN 会向相应服务建立连接，服务方可能处理连接元数据。本项目没有附带自有 TURN 服务或服务商 API 密钥。公开运营应由受控后端签发短期凭据；前端代码无法保密长期密钥。

不要通过关闭浏览器安全策略、关闭系统防火墙或传播长期凭据来掩盖连通问题。先查看错误与网络诊断，区分脚本加载、信令、ICE 和应用握手阶段。
