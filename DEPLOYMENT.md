# 静态部署与联机

## GitHub Pages

把本发布包解压后的文件直接放在仓库根目录，入口文件应叫 `index.html`，不是将 ZIP 本身作为网站文件。

在仓库 **Settings → Pages → Build and deployment** 中选择：

```text
Source: Deploy from a branch
Branch: main（或实际存放文件的分支）
Folder: / (root)
```

保存并等待发布。访问 Pages 设置页给出的网站地址。本项目不需要构建脚本；`.nojekyll` 已随包提供。实际发布方式以 [GitHub 官方文档](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site) 为准。

更新时替换仓库中的 `index.html` 及相关说明；浏览器仍显示旧界面时刷新页面，并检查 Pages 的部署是否完成。连接中的双方建议同时更新到本版本。

## 不搭信令服务器：手动 P2P

进入「联机对战」→「手动 P2P」。本地 HTML 与静态网页使用相同流程，不需要运行本地 Web 服务器。

| 顺序 | 房主 | 加入者 |
| --- | --- | --- |
| 1 | 选择网络范围并生成邀请 | 选择相同的网络范围 |
| 2 | 把完整邀请码发送给对方 | 点击加入者入口并粘贴邀请，生成回应码 |
| 3 | 接收并粘贴回应码，确认连接 | 把完整回应码发回房主，保持页面打开 |
| 4 | 连接成功后开始游戏，控制左拍 | 等待开始，控制右拍 |

连接码较长，可复制，也可保存为文件传递。不能只截取一段字符。生成后关闭、刷新页面或重新生成邀请会使旧连接码失效。

同一局域网模式使用空的 ICE 服务器列表，不调用外部 STUN / TURN；仍可能被 Wi-Fi 客户端隔离、防火墙或浏览器策略阻断。手动传递码只解决协商，不等于连接一定成功。[WebRTC 协商说明](https://webrtc.org/getting-started/peer-connections)

## 使用云端 4 位码

进入「联机对战」→「4 位房间码」。此方式自动交换信令，需要 PeerJS 组件 CDN 及信令服务可达；不是完全无服务器的方式。高级设置支持自建 PeerServer 的域名、端口、路径、key、安全信令及房间分组。

双方使用相同的信令服务与房间分组。公开的 4 位码不具备私密房间密码的安全性。

## 找得到房间，但连不上

网页托管、信令服务、STUN 与 TURN 是不同环节。

| 现象 | 检查方向 |
| --- | --- |
| 页面打不开或显示旧版 | Pages 发布状态、入口文件位置、浏览器缓存 |
| 云组件无法加载，创建不了房间 | 外部组件 CDN、信令服务、HTTPS / WSS 配置 |
| 邀请码 / 回应码无效 | 复制是否完整、是否属于本次连接、页面是否已刷新 |
| 已完成协商但数据通道超时 | 双方网络、防火墙、设备隔离、STUN / TURN |
| 显示 TURN 中继 | 已走中继链路，不应再称为浏览器直连 |

跨网络直连失败时，需要可用的 TURN 服务与有效凭据。TURN 负责中继，不会因为把 HTML 放到 GitHub Pages 上就自动出现。[TURN 官方说明](https://webrtc.org/getting-started/turn-server)

在「高级设置」填写 TURN 地址、用户名与临时凭据；「强制 TURN 中继」仅在有有效 TURN 配置时使用。不要把长期服务凭据提交到公开仓库。本包不提供信令服务器、TURN 服务器或凭据签发后端。

本次验证环境没有提供可用的 WebRTC 连接地址；没有完成公网服务或双设备连接测试。部署后仍需双方在实际网络中验证。
